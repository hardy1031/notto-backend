info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/auth/register
  body:
    type: json
    data: |-
      {
        "user_name": "testuser",
        "email": "test@example.com",
        "password": "password123",
        "first_language": "ja",
        "target_language": "ko"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
